# Database Configuration
# Railway will automatically provide DATABASE_URL for its own PostgreSQL database
# You don't need to set this manually for Railway deployment, it will be provided automatically
DATABASE_URL=postgresql://username:password@localhost/dbname

# Flask Secret Key (used for session management)
# For production, change this to a secure random string
SESSION_SECRET=your_secret_key_here

# Other Configuration
FLASK_ENV=production

# Port Configuration
# Railway will automatically assign PORT for deployment
# For local development, you can use 5000
PORT=5000

# WeasyPrint Configuration
# These are needed for PDF generation
# Railway will provide the necessary dependencies
